---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-sample
  labels:
    app: spring-boot-sample
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-boot-sample
  template:
    metadata:
      labels:
        app: spring-boot-sample
    spec:
      containers:
        - name: spring-boot-sample
          image: <+artifact.image>
          ports:
            - containerPort: 8080